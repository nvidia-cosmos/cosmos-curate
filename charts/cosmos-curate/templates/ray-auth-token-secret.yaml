{{- if .Values.rayAuth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: ray-auth-token
  labels:
    {{- include "curate-ray.labels" . | nindent 4 }}
type: Opaque
stringData:
  token: {{ .Values.rayAuth.token | default (randAlphaNum 64) | quote }}
{{- end }}
